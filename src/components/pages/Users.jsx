import styled from "styled-components";

export const Users = () => {
  return (
    <SUsers>
      <h2>ユーザー一覧</h2>
    </SUsers>
  );
};

const SUsers = styled.div`
  text-align: center;
`;
